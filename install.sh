#!/bin/bash

set -e

shopt -s extglob

# ln -vnsf "$PWD/zsh" "$HOME/.zsh"
# ln -vnsf "$PWD/zshenv" "$HOME/.zshenv"
# ln -vnsf "$PWD/zprofile" "$HOME/.zprofile"
# ln -vnsf "$PWD/zshrc" "$HOME/.zshrc"
#
# ln -vnsf "$PWD/gitconfig" "$HOME/.gitconfig"
# ln -vnsf "$PWD/gitignore_global" "$HOME/.gitignore_global"

#ln -vnsf "$PWD/nvim" "$HOME/.config/nvim"
ln -vnsf "$PWD/ghostty" "$HOME/.config/ghostty"
