# .zshenv - sourced for every zsh invocation: login, interactive, and scripts.
# Keep this to plain variable exports only (no subprocess calls) since it
# also runs for every non-interactive script/subshell.

export GOPATH="$HOME/go"
export RIPGREP_CONFIG_PATH="$HOME/rcfiles/ripgreprc"
